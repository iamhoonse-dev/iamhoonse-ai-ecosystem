/**
 * 웹 애플리케이션의 Next.js 설정
 * 
 * 웹 앱에 특화된 Next.js 설정 파일입니다.
 * 현재는 기본 설정을 사용하고 있으며, 필요에 따라 빌드 최적화,
 * 실험적 기능, 환경변수 등을 추가할 수 있습니다.
 *
 * @type {import('next').NextConfig}
 * @see https://nextjs.org/docs/api-reference/next.config.js/introduction
 */
const nextConfig = {
  // 현재 기본 설정 사용
  // 향후 추가할 수 있는 설정 예시:
  // experimental: { ... },
  // images: { ... },
  // env: { ... }
};

export default nextConfig;
